This is a part of the Enca library (it's "lib", "data" and "tools" directories).
Some files are excluded (only mandatory *.h and *.c files are included).

All files and directories except of the mentioned above are not included.

Some files are modified to avoid compiler warnings.
