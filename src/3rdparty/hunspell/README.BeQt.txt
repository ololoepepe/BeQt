This is a part of the Hunspell library (it's "src/hunspell" directory and
"src/win_api/config.h" file).

All files and directories except of the mentioned above are not included.

File "src/hunspell/affentry.cxx" is modified ("/*...*/" is added at the end).
File "src/hunspell/affixmgr.cxx" is modified (line 1322, unused parameter).
